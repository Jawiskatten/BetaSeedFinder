# Contributing

1. Open an issue before large changes.
2. Keep the public repository layout small and source-focused.
3. Run `./scripts/verify.ps1 -Strict` and `./build.ps1` before submitting a pull request.
4. Native changes must preserve AMD HIP and NVIDIA CUDA compatibility.
5. Do not add Minecraft assets, generated output, binaries, or local paths.
